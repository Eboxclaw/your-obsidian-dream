# ONNX model files

Place model artifacts here at build or onboarding time.

- `all-MiniLM-L6-v2.onnx`
- `tokenizer.json`
- `special_tokens_map.json`

`LFM2-350M-Extract.gguf` is not bundled and should be downloaded during onboarding.
